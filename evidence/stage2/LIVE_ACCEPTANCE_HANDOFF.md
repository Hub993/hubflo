# Annex A Live Acceptance Handoff

## Required environment

- Deploy the exact candidate commit recorded by `git rev-parse HEAD`.
- Record deployment/environment identity and Annex A version 1.0.
- Establish CLIENT_A/CLIENT_B, authorized senders, project authorization,
  ambiguous/unique people and projects, and dedicated feature fixtures from A5.
- Record all generated numeric IDs before execution.
- Keep 9122 inbound-only; do not depend on outbound receipt.

## Required run

Execute all 48 minimum live closure items in Annex A A20 against the same
deployed commit. For every mutating item retain:

1. case ID and exact inbound;
2. timestamp/test ID and final commit;
3. genuine WhatsApp/webhook-arrival evidence;
4. relevant pre-state;
5. authoritative persisted post-state;
6. object ID and mutation count/state transition;
7. PASS/FAIL and defect/rerun reference.

Search/Status require the L inbound route plus controlled exact-result and
persisted no-unintended-mutation evidence. Expiry may use the authority-approved
pre-aged fixture, but the continuation inbound must remain genuinely live.

## Still outstanding

- Every Annex A case whose required class includes L.
- P artifacts from the deployed authoritative database/admin surface for each
  mutating L case.
- Deployed environment identifier and provider/outbound-separation log.
- RTW review and the reserved verdict `PASS — STAGE 2 COMPLETE`.

No outbound 9122 message receipt is required. Webhook 200 must never be used as
mutation evidence. Controlled results in this repository must not be relabelled
as live.

Agent Layer implementation remains prohibited until RTW issues the reserved
Stage 2 verdict and separately locks/authorizes the Agent Layer contract.
